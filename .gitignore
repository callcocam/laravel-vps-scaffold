# Segredos de deploy — NUNCA commitar
vps-deployment/manifest.env
vps-deployment/manifest.*.env
!vps-deployment/templates/manifest.example.env

# Laravel (quando o scaffold for aplicado sobre um app)
/vendor
/node_modules
/.env
/.env.*
!/.env.example
/storage/*.key
/public/build
/public/hot

# SO / editor
.DS_Store
Thumbs.db
